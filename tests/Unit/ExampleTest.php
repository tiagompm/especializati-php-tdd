<?php

namespace Tests;

use Mockery;
use PHPUnit\Framework\TestCase;

class ExampleTest extends TestCase
{
    public function test_example()
    {
       // Mockery::mock(stdClas::class)->sholdReceive('teste')->andReturn(true);
        $this->assertTrue(true);
    }
}
